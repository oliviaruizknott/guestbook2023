import { useCallback, useMemo } from "react";
import { useGetValue, setValue } from "../hooks/useHaxademicStore";

const Calendar = () => {
  return (
    <div className="Calendar">
      <div style={{ background: "red", width: "98vw", height: "200px" }}></div>
    </div>
  );
};

export default Calendar;
